# Bubble-sort-in-C
This C program implements bubble sort on an integer array. It repeatedly compares and swaps adjacent elements, using a flag to detect if the array is already sorted. The algorithm prints the sorted sequence and exits early when no swaps are needed, ensuring efficiency.
